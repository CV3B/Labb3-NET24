<UserControl x:Class="Labb3_NET24.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Labb3_NET24.Views"
             mc:Ignorable="d"
             DataContext="{Binding PlayerViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button HorizontalAlignment="Center" VerticalAlignment="Center" Content="{Binding TestData}" Command="{Binding UpdateButtonCommand}" ></Button>
    </Grid>
</UserControl>
